mutation updateAccount($input: AccountInput!) {
  updateAccount(input: $input) {
    name
  }
}

mutation saveGithubAnalysis {
  saveGithubAnalysis
}

mutation deleteGithubAnalysis($id: String!) {
  deleteGithubAnalysis(id: $id)
}